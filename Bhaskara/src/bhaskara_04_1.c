#include <stdlib.h>
#include <stdio.h>
#include <math.h>
#include "bhaskara.h"

float bhaskara (float a, float b, float c, float *x1, float *x2){

int del;
        
del = pow (y, 2) - (4*x*z);
        
*x2 = (- y - del) / (2 * x);
*x1 = (- y +del) / (2 * x);
        
return (2);
}
